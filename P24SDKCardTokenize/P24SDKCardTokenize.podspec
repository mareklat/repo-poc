Pod::Spec.new do |spec|
    spec.name                     = 'P24SDKCardTokenize'
    spec.version                  = '0.0.1'
    spec.homepage                 = 'https://p24.pl'
    spec.source                   = { :http=> ''}
    spec.authors                  = ''
    spec.license                  = ''
    spec.summary                  = 'P24 SDK for card tokenization'
    spec.vendored_frameworks      = 'P24SDKCardTokenize.xcframework'
    spec.libraries                = 'c++'
    spec.ios.deployment_target = '12.0'
                
                
                
                
                
end